import EarlyAccess from '../components/EarlyAccess';

export const metadata = {
  title: 'Login - Early Access | INFRAA',
  description: "We're building the procurement infrastructure layer for modern construction.",
};

export default function LoginPage() {
  return <EarlyAccess />;
}
